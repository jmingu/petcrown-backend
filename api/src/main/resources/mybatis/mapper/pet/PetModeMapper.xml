<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.api.pet.mapper.PetModeMapper">

    <!-- 펫 감정 모드 전체 목록 조회 -->
    <select id="selectAllPetModes" resultType="kr.co.api.pet.dto.command.PetModeInfoDto">
        /*
        PetModeMapper.xml
        selectAllPetModes
        펫 감정 모드 전체 목록 조회
        */
        SELECT
            pet_mode_id as petModeId,
            mode_name as modeName
        FROM pet_mode
        WHERE delete_date IS NULL
        ORDER BY pet_mode_id
    </select>

</mapper>
