server:
  servlet:
    context-path: /
  port: 8080
spring:
  profiles:
    active:
      - local
    include:
      - db

logging:
  level:
    com:
      user: debug
    org:
      hibernate:
        orm:
          jdbc:
            bind: trace